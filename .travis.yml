language: java
sudo: false
cache:
  directories:
  - $HOME/.m2

install:
  - mvn install -Ptravis -DskipTests=true -Dmaven.javadoc.skip=true -Dlog4j.configuration= -Dvalidate.silent=true -B -V -q

script:
  - mvn test -Dlog4j.configuration= -Dvalidate.silent=true -q
